<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SmotrimAnime" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="android:forceDarkAllowed" tools:targetApi="LOLLIPOP">true</item>
        <item name="android:textColorHint">@color/gray_finish</item>
        <item name="buttonStyle">@style/ButtonNightStyle</item>
        <item name="android:textColor">@color/white</item>
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>
</resources>