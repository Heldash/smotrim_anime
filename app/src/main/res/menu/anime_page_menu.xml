<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:title="favorite"
        android:id="@+id/fav_menu_btn"
        android:icon="@drawable/empty_favorite"
        app:showAsAction="always"/>
    <item android:title="izbran"
        android:id="@+id/izbran_menu_btn"
        android:icon="@drawable/bookmark_empty"
        app:showAsAction="always"/>
    <item android:title="watched"
        android:id="@+id/watched_menu_btn"
        android:icon="@drawable/watched"
        app:showAsAction="always"/>
</menu>